---
import { skillGroups } from '../data/skills.ts';
---
<section id="skills" class="border-t border-hairline/10 py-20">
  <h2 class="font-mono text-sm font-normal text-accent">04 / Skills</h2>
  <div class="reveal mt-8 grid gap-8 md:grid-cols-2">
    {skillGroups.map((g) => (
      <div>
        <h3 class="font-mono text-sm text-ink">{g.group}</h3>
        <ul class="mt-3 flex flex-wrap gap-2">
          {g.items.map((it) => (
            <li class="rounded-md border border-hairline/15 bg-hairline/5 px-3 py-1 font-mono text-xs text-muted">{it}</li>
          ))}
        </ul>
      </div>
    ))}
  </div>
</section>
